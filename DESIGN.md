# 蜜蜂关键点标注软件设计与使用说明

## 1. 目标

本软件用于在已有 `bee` 目标检测框的基础上标注蜜蜂关键点，兼容
X-AnyLabeling 3.3.10 的 JSON 格式，并保留原有矩形框。

默认数据包：

```text
C:\BeeAnnotationData\标注员_01\B-5-3_区段_04
```

主要能力：

1. 选择任意包含图片和同名 JSON 的文件夹。
2. 放大显示当前 `bee` 框，便于精确标注关键点。
3. 显示当前图片的整图鸟瞰，包含所有检测框和关键点。
4. 默认关键点标签为 `head`、`tail`，允许新增、重命名和移除可选标签。
5. 可手动开启“中心对称”：标注 `head` 时，以当前检测框中心为对称中心自动生成 `tail`。
6. 可通过类似音量条的滑块，把自动尾点比例调节为 `0%～200%`。
7. 关闭中心对称后，可以逐个标注任意关键点。
8. 任意选择两张图片，输入 IoU 阈值，将源图关键点按框内相对位置传播到目标图的匹配框。
9. 支持键盘和鼠标侧键的双槽位快捷键自定义、冲突检查和持久化。
10. 内置完整使用帮助，可通过右上角“帮助”或 `F1` 打开。
11. 自动保存、原 JSON 备份和撤销。

## 2. 数据调研结论

目标包共有 15 张 JPG 图片，分辨率均为 `1920×1080`。每张图片存在同名 JSON，
格式为 X-AnyLabeling/LabelMe：

```json
{
  "version": "3.3.10",
  "flags": {},
  "shapes": [
    {
      "label": "bee",
      "points": [[x1, y1], [x2, y1], [x2, y2], [x1, y2]],
      "group_id": null,
      "shape_type": "rectangle"
    }
  ],
  "imagePath": "B-5-3_frame_004465.jpg",
  "imageData": null,
  "imageHeight": 1080,
  "imageWidth": 1920
}
```

当前 15 个 JSON 只包含 `bee` 矩形框，单张约 331～345 个框。因此软件不会重新检测，
只读取已有框并添加 `point` 类型关键点。

## 3. 目录结构

```text
bee_keypoint_annotator/
├─ DESIGN.md                 # 本设计与使用说明
├─ app.py                    # Tkinter 可视化界面
├─ annotation_io.py          # X-AnyLabeling JSON 读写、备份和关联
├─ core.py                   # IoU、中心对称、坐标迁移等纯算法
├─ settings.json             # 默认标签和界面设置
├─ run_keypoint_annotator.bat# Windows 启动入口
└─ tests/
   └─ test_core.py           # 核心算法测试
```

运行环境使用本机已有的 Python 3.9、Tkinter、Pillow 和 NumPy，不额外安装大型 GUI 框架。

## 4. 界面设计

### 4.1 顶部工具栏

从左到右：

- 打开文件夹、保存。
- 上一张/下一张图片。
- 当前关键点标签。
- 标签管理。
- 中心对称开关。
- 对称源标签与对称目标标签，默认 `head → tail`。
- 对称比例滑块，范围 `0%～200%`，默认 `100%`。
- 快捷键设置。
- 使用帮助。
- IoU 传播。

### 4.2 左侧：当前框放大视图

- 自动裁剪当前矩形框，并留少量上下文边距。
- 单击添加当前标签的关键点。
- 开启中心对称且当前标签等于对称源标签时，同时生成对称目标点。
- 开启中心对称时，右键只手动标注对称目标标签，默认是 `tail`，用于修正自动对称点。
- 关闭中心对称时，右键删除离鼠标最近的当前框关键点。
- 鼠标中键删除鼠标附近的关键点；附近没有点时不会删除其他位置的点。
- 每次新增、修正或删除关键点后立即自动保存 JSON。
- 黄色边框表示当前选中的检测框。

### 4.3 右侧：整图鸟瞰

- 显示完整图片、所有 `bee` 框和所有关键点。
- 单击某个矩形框后，将其设为当前框并在左侧放大。
- 当前框使用黄色粗线，其余框使用蓝色细线。
- `head` 默认使用红色，`tail` 默认使用青色；新增标签使用稳定的循环颜色。

### 4.4 底部状态区

- 当前文件序号、当前框序号、当前框已有关键点。
- 上一个框、下一个框。
- 删除当前标签点、删除当前框全部关键点。
- 保存状态和操作提示。

## 5. 标注数据模型

### 5.1 关键点格式

新增点保持 X-AnyLabeling 可读：

```json
{
  "kie_linking": [],
  "label": "head",
  "score": null,
  "points": [[421.5, 306.2]],
  "group_id": 12,
  "description": "",
  "difficult": false,
  "shape_type": "point",
  "flags": {},
  "attributes": {}
}
```

### 5.2 检测框与关键点关联

- 第一次给某个框添加关键点时，为该矩形框分配当前图片内唯一的整数 `group_id`。
- 该框的所有关键点使用相同 `group_id`。
- 读取旧关键点时，优先按 `group_id` 关联。
- 若旧点没有 `group_id`，使用“包含该点且面积最小的矩形框”进行推断。

这样既能被本软件稳定识别，也能继续使用 X-AnyLabeling 打开和编辑。

## 6. 中心对称算法

设检测框边界为：

```text
(x1, y1) -------- (x2, y1)
   |                  |
   |                  |
(x1, y2) -------- (x2, y2)
```

框中心：

```text
cx = (x1 + x2) / 2
cy = (y1 + y2) / 2
```

人工点击头部点 `H(hx, hy)`，比例为 `k` 后，尾部点为：

```text
T(tx, ty)
tx = cx + k * (cx - hx)
ty = cy + k * (cy - hy)
```

其中：

- `k = 1.0`（100%）：严格中心对称。
- `k < 1.0`：尾点更靠近框中心。
- `k > 1.0`：尾点离框中心更远。
- `k = 0`：尾点位于框中心。
- 计算结果超出检测框时限制在框边界内。
- 滑块只影响之后自动生成的尾点，不修改已有关键点。

只有同时满足以下条件才自动生成：

1. “中心对称”开关已开启。
2. 当前标签等于对称源标签，默认是 `head`。
3. 点击位置位于当前检测框内部。

关闭开关后，每次点击只添加当前标签，适合特殊姿态或非严格对称的蜜蜂。

## 7. 任意两图 IoU 传播

### 7.1 框匹配

用户选择：

- 源图片：已经标好关键点。
- 目标图片：需要接收关键点。
- IoU 阈值：`0～1`。

只比较标签相同的矩形框。两个框的 IoU：

```text
IoU = 交集面积 / 并集面积
```

将所有达到阈值的候选框对按 IoU 从高到低排序，再进行一对一贪心匹配：

1. 每个源框最多匹配一个目标框。
2. 每个目标框最多被一个源框匹配。
3. 优先保留 IoU 最大的匹配。

该方法适合本项目每 5 帧取一帧、相邻画面变化较小的连续帧。

### 7.2 关键点相对位置迁移

源框边界为 `(sx1, sy1, sx2, sy2)`，源关键点为 `(px, py)`：

```text
u = (px - sx1) / (sx2 - sx1)
v = (py - sy1) / (sy2 - sy1)
```

目标框边界为 `(tx1, ty1, tx2, ty2)`，目标关键点：

```text
new_x = tx1 + u * (tx2 - tx1)
new_y = ty1 + v * (ty2 - ty1)
```

因此传播的是“关键点在框内的相对位置”，不是原图绝对坐标。

### 7.3 覆盖策略

- 默认“只补缺失点”：目标框已有同名关键点时跳过，保护人工标注。
- 可勾选“覆盖目标框同名关键点”。
- 点击“预览”只计算数量，不修改文件。
- 点击“应用”后才写入目标图片 JSON。
- 第一次修改某个 JSON 时自动生成同目录 `.bak` 备份。

## 8. 标签管理

默认标签：

```text
head
tail
```

支持：

- 新增：加入可选标签列表。
- 重命名：同时重命名当前文件夹中已有的同名 `point` 标注。
- 移除：只从可选列表移除，不删除已经存在的标注点。
- 从已有 JSON 读取到的关键点标签会自动加入可选列表。

检测框标签 `bee` 不通过关键点标签管理器修改。

## 9. 快捷键

点击顶部“快捷键”可自定义。单击一个快捷键框，待它显示“请按键或侧键…”后，
直接按下键盘按键、组合键或鼠标侧键，软件会自动识别并填入；右键单击快捷键框可
清空。每个动作可以同时设置两个快捷键，例如保留键盘 `Q/E` 的同时使用鼠标侧键。
重复分配同一快捷键时软件会提示冲突，不会保存错误配置。设置保存在
`settings.json`，下次启动继续生效。鼠标侧键只在本软件窗口内生效。

默认配置：

| 快捷键 | 功能 |
|---|---|
| `Ctrl+O` | 打开数据文件夹 |
| `Ctrl+S` | 保存当前图片标注 |
| `A` | 上一张图片 |
| `D` | 下一张图片 |
| `Q` / 鼠标侧键 1 | 上一个检测框 |
| `E` / 鼠标侧键 2 | 下一个检测框 |
| `1` | 切换到 `head` |
| `2` | 切换到 `tail` |
| `Ctrl+M` | 开启/关闭中心对称 |
| `Ctrl+I` | 打开任意两图 IoU 传播窗口 |
| `Ctrl+L` | 打开标签管理 |
| `Ctrl+K` | 打开快捷键设置 |
| `F1` | 打开使用帮助 |
| `Ctrl+Z` | 撤销当前图片上一次点标操作 |
| `Delete` | 删除当前框的当前标签点 |
| `Shift+Delete` | 删除当前框的全部关键点 |
| `Ctrl+T` | 显示/隐藏关键点标签名 |
| `Ctrl+B` | 显示/隐藏非当前检测框 |

鼠标操作：

| 操作 | 功能 |
|---|---|
| 鸟瞰图左键 | 选择检测框 |
| 放大图左键 | 添加当前标签关键点 |
| 放大图右键（中心对称开启） | 手动添加或修正对称目标点，默认 `tail` |
| 放大图右键（中心对称关闭） | 删除最近关键点 |
| 放大图中键 | 删除鼠标附近的关键点 |
| 鼠标滚轮 | 切换上一个/下一个检测框 |

## 10. 使用帮助

点击右上角“帮助”或按 `F1` 打开。帮助窗口包含：

- 软件用途与界面区域说明。
- 推荐关键点标注流程。
- 左键、右键、中键、滚轮和鼠标侧键说明。
- 中心对称比例算法和使用方式。
- 自动保存、撤销与 `.json.bak` 备份说明。
- 任意两图 IoU 传播步骤。
- 标签管理和快捷键自定义方法。
- 当前实际快捷键列表和颜色说明。

帮助内容在软件内部显示，不会打开浏览器，也不会影响当前标注。

## 11. 保存与安全

1. 每次新增、修正或删除关键点后立即保存 JSON；图片切换、文件夹切换和关闭软件时也会检查未保存内容。
2. JSON 采用临时文件替换方式写入，避免写到一半损坏。
3. 第一次覆盖原 JSON 前创建 `原文件名.json.bak`。
4. 原有字段和原有矩形框全部保留，只更新必要的 `group_id` 并添加/修改关键点。
5. 软件不修改 JPG 图片。

## 12. 使用方法

双击：

```text
run_keypoint_annotator.bat
```

首次打开会优先尝试加载默认数据包。也可以点击“打开文件夹”选择其他标注包。

推荐工作流：

1. 在右侧鸟瞰图选择一个 `bee` 框。
2. 打开“中心对称”，当前标签选择 `head`。
3. 根据蜜蜂姿态调节对称比例，默认 `100%`。
4. 在左侧放大图点击蜜蜂头部，软件按当前比例自动生成 `tail`。
5. 如果自动生成的尾部位置不准确，直接在正确尾部位置单击右键，覆盖该框的 `tail`。
6. 按 `E` 或滚轮切换到下一个框。
7. 特殊姿态时也可关闭中心对称，分别选择标签逐个标记。
8. 完成一张图片后按 `D`，软件自动保存并进入下一张。
9. 要复用关键点时按 `Ctrl+I`，选择源图、目标图和 IoU 阈值，先预览再应用。
10. 必要时回到目标图逐框复审。

## 13. 验收标准

- 能打开指定 15 张图片的数据包。
- 每张图能完整加载 300 多个已有 `bee` 框。
- 鸟瞰图可显示所有框和关键点，并可点击选择框。
- 放大图点击后能写入 `head` 点。
- 开启中心对称后能同时写入对称的 `tail` 点。
- 关闭中心对称后可逐点标注。
- 任意两图之间可预览并执行 IoU 传播。
- 传播坐标使用框内相对位置，目标已有点默认不会被覆盖。
- 保存后的 JSON 能再次被本软件和 X-AnyLabeling 正常读取。
- 快捷键和自动保存正常工作。
